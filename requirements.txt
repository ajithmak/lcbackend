# ─── Core Django ──────────────────────────────────────────────────────────────
Django==4.2.11
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1

# ─── PostgreSQL driver ────────────────────────────────────────────────────────
# Use binary wheel — no compilation needed on Render
psycopg2-binary==2.9.9

# ─── Utilities ───────────────────────────────────────────────────────────────
django-cors-headers==4.3.1
# Pillow 10.4.0 has wheels for Python 3.11 on Linux (Render's environment)
Pillow==10.4.0
python-dotenv==1.0.1

# ─── Production server ────────────────────────────────────────────────────────
gunicorn==21.2.0
whitenoise==6.7.0

# ─── Cloud image storage (Cloudinary — free 25GB, survives Render restarts) ───
cloudinary==1.40.0
django-cloudinary-storage==0.3.0
openpyxl==3.1.5      # XLSX import support
